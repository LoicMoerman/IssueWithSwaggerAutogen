# IssueWithSwaggerAutogen

To start project juste make :

```bash
npm install 
npm start
```

To reproduce the bug, uncomment the line in src/routes/index.ts and relaunch the app two times to regenerate the documentation.
